# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# .vscodeディレクトリは除外しません（チーム共有のためコミット対象）

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Electron パッケージング出力（配布に不要なものを除外）
# 最終配布物（.exe / .dmg / .zip など）は残し、
# 中間生成物やデバッグ用ファイルは無視します。
dist/*-unpacked/
dist/win-unpacked/
dist/linux-unpacked/
dist/mac/
dist/mac-x64/
dist/mac-arm64/
dist/builder-debug.yml
dist/builder-effective-config.yaml
dist/*.blockmap
